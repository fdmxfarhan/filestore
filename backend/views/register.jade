doctype html

html
    head
        title فایل استور
        link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css")
        link(rel="shortcut icon", href="/img/logo.png", type="image/x-icon")
        script(src="/js/jquery-3.5.1.min.js")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        //- meta(name="theme-color" content="#050507")
        //- script(src="/js/index.js")
        link(rel="stylesheet", href="/css/layout.css")
        link(rel="stylesheet", href="/css/register.css")

        block styles
    body
        .black-modal
        //- include ./components/navbar
        .logo 
            img(src="/img/logo.png")
        .register-view 
            h1 عضویت در فایل استور
            p پس از تکمیل فرم، اطلاعات ورود به اپلیکیشن به شماره ثبت شده پیامک می‌شود.
            form(action="/register-estate2" method="POST") 
                .text-input-view 
                    label(for="name") نام مشاور املاک: 
                    input(type="text" name="name" placeholder="نام و نام خانوادگی")
                .text-input-view 
                    label(for="address") آدرس: 
                    input(type="text" name="address" placeholder="نام و نام خانوادگی")
                .text-input-view 
                    label(for="phone") شماره تلفن: 
                    input(type="text" name="phone" placeholder="شماره تلفن")
                button.submit(type="submit") ثبت
        //- include ./components/footer
        script(src="/js/numberFixer.js") 
